This project uses [Node.js Express](https://expressjs.com/en)to compile SASS (SCSS file).

Generation of package.json file):
$ npm init 

$ npm install express --save

$ npm install node-sass-middleware --save

